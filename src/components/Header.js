import React from 'react'

function Header() {
    return (
        <header style={headerStyle}>
            TODO REACT APP
        </header>
    )
}

const headerStyle = {
    width: '100%',
    height: '4rem',
    size: '2rem',
    backgroundColor: '#333',
    color: '#FFFFFF',
    textAlign: 'center',
}

export default Header
